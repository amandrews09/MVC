<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav>
    <!-- Navigation content -->
  </nav>
  <main>
    <h1>Welcome to the Blog</h1>
    {{#if posts.length}}
      {{#each posts}}
        <div>
          <h2>{{this.title}}</h2>
          <p>{{this.content}}</p>
          <p>Posted by {{this.user.username}}</p>
        </div>
      {{/each}}
    {{else}}
      <p>No posts found.</p>
    {{/if}}
    <pre>{{{json posts}}}</pre> <!-- Debugging: Show the posts data -->
  </main>
</body>
</html>
